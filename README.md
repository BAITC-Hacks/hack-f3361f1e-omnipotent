# Sana — от идеи к делу

Рабочий прототип для AI Sana / HackAlem: бизнес уточняет задачу, подтверждает карточку и публикует её в каталоге. Студенты откликаются, бизнес вручную выбирает одну, несколько или ни одной команды.

## Быстрый запуск

Нужен **Node.js 22.12+** (или Node.js 24 LTS) и npm. Отдельная база данных и API-ключ не нужны.

```bash
git clone https://github.com/BAITC-Hacks/hack-f3361f1e-omnipotent.git
cd hack-f3361f1e-omnipotent
npm ci
npm run dev
```

Репозиторий приватный: для клонирования нужна авторизация GitHub. Если он уже клонирован, просто перейдите в его папку и выполните последние две команды.

Откройте **http://localhost:5173**. API запускается на `127.0.0.1:3001`, Vite перенаправляет `/api` на него. Остановка — `Ctrl+C`. Порты 5173 и 3001 должны быть свободны.

Проверка и сборка:

```bash
npm test
npm run build
npm start
```

После сборки `npm start` отдаёт API и интерфейс вместе: **http://localhost:3001**. `npm run build` сам сервер не запускает. В обычном `npm start` нужны установленные devDependencies, поскольку минимальный сервер запускается через `tsx`.

Для другого порта собранного приложения: `PORT=3002 npm start` (macOS/Linux) или `$env:PORT=3002; npm start` (PowerShell). В режиме разработки используйте API-порт 3001. `.env.example` — справочный пример; автоматическое чтение `.env` не подключено.

## Что работает

- Русский интерфейс с демонстрационным переключением ролей «Бизнес» / «Команда».
- Описание потребности, уточняющие вопросы, редактируемая карточка и ручное подтверждение.
- Рейтинг 0–100 с разбором и подсказками; неподтверждённые сведения не дают баллов.
- Общий каталог опубликованных задач с сортировкой, поиском и фильтрами.
- Отклики команд: идея, план, срок и ссылка на прототип.
- Ручной выбор/отклонение предложений, несколько выбранных команд одновременно.
- Подтверждение выполненного этапа: +10 баллов команде однократно за отклик.
- Сохранение данных на сервере между перезапусками.

## Архитектура

React + TypeScript + Vite — интерфейс. Node.js + Express — API. Общие типы и детерминированная формула рейтинга используются в проекте. Для небольшого однопроцессного прототипа данные хранятся в JSON с атомарной записью.

```text
src/             интерфейс React и стили
shared/          типы данных и формула рейтинга
server/          API, хранилище, синтетические данные, AI-заглушка
tests/           проверки рейтинга и серверных сценариев
data/store.json  рабочие данные, создаются при первом запуске (не в Git)
```

`Task`: описание, отрасль, десять полей карточки, подтверждение, публикация. `Team`: название, интересы, навыки, технологии, баллы прогресса. `Proposal`: задача, команда, идея, план, срок, ссылка, решение бизнеса, подтверждённый этап.

## Формула рейтинга

| Категория | Баллы | Условие |
|---|---:|---|
| Контекст и потребность | 20 | Оба поля заполнены |
| Данные и материалы | 20 | Поле заполнено |
| Ожидаемый результат | 15 | Поле заполнено |
| Критерии успеха | 15 | Поле заполнено |
| Ограничения | 10 | Поле заполнено |
| Пользователи | 10 | Поле заполнено |
| Связь с бизнесом | 10 | Контакт и формат взаимодействия заполнены |

Баллы начисляются только после ручного подтверждения карточки. Каждая категория начисляется целиком либо даёт 0; пробелы не считаются содержанием. Название нужно для удобства каталога, но не приносит баллы. Изменение формы снимает подтверждение; для сохранения новой подтверждённой версии нужно подтвердить её заново.

Уровни: **0–39 — Черновик**, **40–69 — Рабочая**, **70–89 — Готовая**, **90–100 — Приоритетная**. Это рейтинг полноты подтверждённых сведений, а не автоматическая экспертиза их достоверности.

Низкий рейтинг не запрещает публикацию или отклик. Неопубликованный черновик и уровень готовности «Черновик» — разные понятия. Все опубликованные задачи доступны всем командам. Выбор одной команды не отклоняет остальные предложения автоматически. Баллы команд не влияют на рейтинг бизнес-задач.

## AI: демонстрационный режим

**В этой версии нет вызова внешней модели.** Используется явно обозначенная локальная заглушка, разрешённая условиями кейса. Она анализирует незаполненные поля, возвращает минимум три уточняющих вопроса и переносит только предоставленные пользователем сведения. Исходное описание может быть перенесено в контекст дословно. Сроки, данные, метрики, контакты и другие факты не выдумываются.

Промпт, построение вопросов, формат результата и проверка некорректного ответа находятся в `server/ai.ts`. Принцип промпта: «Помоги уточнить бизнес-задачу; задай минимум три вопроса по недостающим сведениям; используй только входные факты; верни структурированный JSON; не выбирай команду».

Контракт `POST /api/clarify`:

```json
{
  "description": "Наш магазин часто остаётся без популярных товаров",
  "industry": "Ритейл",
  "fields": { "data": "Есть CSV продаж за 6 месяцев" }
}
```

Результат:

```json
{
  "mode": "demo",
  "notice": "Сообщение о демонстрационном режиме",
  "questions": [
    { "field": "need", "question": "Какое изменение требуется?" },
    { "field": "users", "question": "Кто будет пользоваться решением?" },
    { "field": "success", "question": "Как измерить успешность?" }
  ],
  "fields": {
    "title": "", "context": "Наш магазин часто остаётся без популярных товаров",
    "need": "", "users": "", "data": "Есть CSV продаж за 6 месяцев",
    "constraints": "", "outcome": "", "success": "", "contact": "", "interaction": ""
  }
}
```

Тексты и порядок вопросов зависят от недостающих сведений. Некорректный ввод API даёт понятную ошибку; результат генерации проверяется и при неверной структуре заменяется безопасными вопросами. Перед подключением реальной модели нужно добавить серверный провайдер и его ключ, сохранив этот контракт и ручное подтверждение. Реальный AI — следующий этап, не скрытая зависимость запуска.

## Демонстрация за пять минут

1. В режиме бизнеса создайте задачу: «Наш магазин часто остаётся без популярных товаров».
2. Получите вопросы и добавьте данные: CSV продаж за полгода, пользователи — менеджеры закупок, результат — прототип прогноза спроса, критерий — снижение дефицита на 15%, ограничение — две недели, контакт — `demo@example.org`, консультации — раз в неделю.
3. Покажите редактируемую карточку, подтвердите часть сведений и сохраните. Дополните остальные поля, подтвердите заново и покажите рост рейтинга.
4. Опубликуйте задачу и найдите её в каталоге.
5. Переключитесь в режим команды, отправьте идею, план, срок и демонстрационную ссылку `https://example.org/prototype`.
6. Вернитесь в режим бизнеса, выберите предложение вручную; подтвердите выполненный этап и покажите начисление баллов.

## Данные и проверки

При первом запуске создаются пять исходных описаний, пять карточек с разной полнотой, пять команд и пять предложений. Все примеры синтетические; ссылки `example.org` и контакты — демонстрационные.

`npm test` включает сквозной DOM-тест интерфейса (создание → подтверждение → публикация → отклик → выбор → результат), а также проверяет начисление баллов, подтверждение/публикацию, доступность слабых задач для откликов, ручной выбор, однократные баллы прогресса, ошибки запросов и сохранение данных. Для сброса своих демонстрационных данных остановите сервер и переименуйте `data/store.json` в резервную копию — следующий запуск создаст исходный набор.

## Границы прототипа

Переключатель роли не является авторизацией: все демонстрационные бизнес-задачи управляются из одного режима. Нет регистрации, реального AI-провайдера, загрузки файлов, чата и интеграций. JSON-хранилище предназначено для одного серверного процесса. Приложение по умолчанию слушает только локальный компьютер; публичное развёртывание и разграничение доступа — отдельный этап.
