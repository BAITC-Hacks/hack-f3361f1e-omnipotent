# Copy to .env, insert your key locally, then restart the server.
# The server loads .env; never use a VITE_ prefix for secrets.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# npm run dev expects API port 3001.
PORT=3001
