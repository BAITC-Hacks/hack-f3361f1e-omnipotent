# Optional production API/UI port; npm run dev expects API port 3001.
PORT=3001
# This first version uses an explicitly labeled local AI demonstration stub.
# No API key is needed. Environment variables are read from the shell, not this file.
